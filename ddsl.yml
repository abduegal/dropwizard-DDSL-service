zookeeper:
  tickTime: 2000
  initLimit: 0
  syncLimit: 0
  clientHost: localhost
  clientPort: 2181
  dataDir: zookeeperData
  dataLogDir: zookeeperLog

configwriter:
  destination: generatedConfig.conf
  shellCommand: nginx -s reload
  secondsBetweenChecks: 10

http:
  port: 0
  adminPort: 0
